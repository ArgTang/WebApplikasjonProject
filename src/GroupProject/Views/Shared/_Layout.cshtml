<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/style.css" />
    
    <environment names="Development">
       
    </environment>
    <environment names="Staging,Production">
        <!-- CDN links here see https://docs.asp.net/en/latest/client-side/bootstrap.html -->
    </environment>
</head>
<body>

    @await Html.PartialAsync("navigation")
    
    <!-- Typescript integration test, remove later-->
    <p class="message"></p>
    Compiler: <input id="compiler" value="TypeScript" onkeyup="document.querySelector('.message').innerText = sayHello()" />

    <main>
        <!-- this is where content is inserted -->
        @RenderBody()
    </main>

    @await Html.PartialAsync("footer")

    <script src="JS/app.js"></script>
    <environment names="Development">

    </environment>
    <environment names="Staging,Production">

    </environment>

    @RenderSection("scripts", required: false)
</body>
</html>
